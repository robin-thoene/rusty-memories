{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "build:rusty-memories",
      "command": "cargo",
      "args": [
        "build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
